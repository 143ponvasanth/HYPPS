import { Typography } from '@mui/material';

const SearchClasses = () => {
    return (
        <div>
            <Typography variant="h4" gutterBottom>
                Search Classes
            </Typography>
            <Typography variant="body1">
                This is the Search Classes page.
            </Typography>
        </div>
    );
};

export default SearchClasses;